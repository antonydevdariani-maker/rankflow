import RankFlowDashboard from "@/components/RankFlowDashboard";

/**
 * Root route "/": RankFlow dashboard (blog cards + SEO chat).
 * Client UI lives in components/RankFlowDashboard.tsx.
 */
export default function HomePage() {
  return <RankFlowDashboard />;
}
