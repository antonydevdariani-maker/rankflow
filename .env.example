GROQ_API_KEY=
GITHUB_TOKEN=
# GITHUB_BRANCH=main

# Where the live site shows posts (AutoRank uses /blog/{slug}). After publish, this opens in a new tab.
# Use your production URL when the dashboard runs locally, or http://localhost:3000 when AutoRank dev is on 3000.
NEXT_PUBLIC_VIEW_SITE_URL=http://localhost:3000
